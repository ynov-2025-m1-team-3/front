{
  "name": "spa-server",
  "version": "1.0.0",
  "description": "Simple server for React SPA on Render",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "npm run build:react",
    "build:react": "vite build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "path": "^0.12.7"
  },
  "engines": {
    "node": ">=18"
  }
}
